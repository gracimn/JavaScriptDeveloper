import { createContext } from 'react';

const AppContext = createContext();

export default AppContext;

//Este código é uma prática comum em aplicações React para criar e exportar contextos que serão utilizados para compartilhar estados ou funcionalidades específicas em toda a aplicação.